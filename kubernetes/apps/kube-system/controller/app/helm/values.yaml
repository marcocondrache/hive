nameOverride: controller

networking:
  enabled: false
  clusterCIDR: 10.42.0.0/16
  network:
    valueFrom:
      secretKeyRef:
        name: controller-secret
        key: network-id

env:
  HCLOUD_TOKEN:
    valueFrom:
      secretKeyRef:
        name: controller-secret
        key: token

monitoring:
  enabled: false
